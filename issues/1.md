Implement ids-metrics/sampler network metrics system (server and client)

**Description:** Implement the ids-metrics/sampler component composed of Server (Rust eBPF/XDP monitoring daemon capturing TCP/HTTP/HTTPS, per-CPU counters, REST API/dashboard), Client (Rust traffic generator with TOML config + CLI overrides for bandwidth/connections/latency/duration), Documentation (README updates). Exclude CI/CD and automated tests.